import array
import numpy as np
import tensorflow as tf
from collections import defaultdict


def load_vacab(filename):
	pass

def load_glove_vectors(filename, vocab):
	pass

def build_initial_embedding_matrix(vocab_dict, glove_vectors, embedding_dim):
	pass